#include<stdio.h>
void findElements(int arr[], int n, int *largest, int secoundlarges, int smallest) {
    *largest = *secoundlargest = arr[0];
    *smallest = arr[0];
    for (int i = 0; i < n; i++){
        if arr[i] < smallest
    }
}








hello 
